#!/bin/bash

S_DIR_config=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

source $S_DIR_config/config/paths.sh
source $S_DIR_config/config/fonts.sh
source $S_DIR_config/config/styling.sh
